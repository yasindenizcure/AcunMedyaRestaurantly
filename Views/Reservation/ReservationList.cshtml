@model List<AcunMedyaRestaurantly.Entities.Reservation>
@{
    ViewBag.Title = "ReservationList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}
<div class="row page-titles mx-0">
    <div class="col p-md-0">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="javascript:void(0)">Anasayfa</a></li>
            <li class="breadcrumb-item active"><a href="javascript:void(0)">Rezervasyon Listesi</a></li>
        </ol>
    </div>
</div>
<!-- row -->

<div class="container-fluid">
    <form method="get">
        <div class="form-group">
            <input type="text" name="searchText" placeholder="Aranacak Kelime" class="form-control" />
            <button type="submit" class="btn btn-dark gradient-9">Ara</button>
        </div>
    </form>
    <div class="row">

        <!-- /# column -->
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <div class="card-title">
                        <h4>Rezervasyon</h4>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>Ad</th>
                                    <th>Email</th>
                                    <th>Telefon</th>
                                    <th>Açıklama</th>
                                    <th>Rezervasyon Tarihi</th>
                                    <th>Saat</th>
                                    <th>Misafir Sayısı</th>
                                    <th>Rezervasyon Durumu</th>
                                    <th>Sil</th>
                                    <th>Güncelle</th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <th>@item.Name</th>
                                        <td>@item.Email</td>
                                        <td>@item.Phone</td>
                                        <td>@item.Description</td>
                                        <td>@item.ReservationDate</td>
                                        <td>@item.Time</td>
                                        <td>@item.GuestCount</td>
                                        <td>@item.ReservationStatus</td>
                                        <td>
                                            <a href="/Reservation/ReservationDelete/@item.ReservationId" class="btn btn-danger">
                                                <i class="fa fa-close color-muted m-r-5"></i>Sil
                                            </a>
                                        </td>

                                        <td><a href="/Reservation/ReservationEdit/@item.ReservationId" class="btn btn-info gradient-2">Güncelle</a></td>

                                    </tr>
                                }


                            </tbody>
                        </table>
                        <a href="/Reservation/ReservationCreate" class="btn btn-success">Rezervasyon Ekle</a>
                    </div>
                </div>
            </div>
        </div>
        </div>
        <!-- /# column -->

    </div>
